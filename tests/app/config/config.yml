framework:
    secret: 'test_test_test'
    router: ~
    default_locale: en
    session: ~
    profiler: { only_exceptions: false }
    test: ~
    session:
        storage_id: session.storage.filesystem
